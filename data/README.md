Este directorio se llena localmente con `scripts/download_dataset.py`.

El dataset no se versiona en GitHub porque supera por mucho el tamaño razonable para el repositorio. El script crea dos enlaces locales:

- `data/male`
- `data/female`
